import React from 'react'

const SubModule = ()  => {
    return(
        <div className='submodule'>
            <h1>Submodule</h1>
            <p>This is the most basic submodule one can think of. It just presents some text in HTML elements</p>
        </div>
    )
}

export default SubModule